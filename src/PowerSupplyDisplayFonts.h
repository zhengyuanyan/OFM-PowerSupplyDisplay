#pragma once

/* ============================================================================
 *  本模块使用的中文字库（由 tools/gen-fonts.ps1 生成，见 README）
 * ----------------------------------------------------------------------------
 *  字库只包含界面实际用到的汉字 + ASCII，所以体积很小。
 *  改了界面文案后必须重新生成，否则新字符会显示成方框。
 * ========================================================================== */

#include <lvgl.h>

LV_FONT_DECLARE(lv_font_ps_16)
LV_FONT_DECLARE(lv_font_ps_20)
