### 背光亮度

屏幕背光的 PWM 占空比，取值 **10 ~ 100 %**。

* 100 % = 最亮，适合调试或光线很强的机房；
* 30 ~ 60 % 在正常室内环境下已经足够亮，且能明显降低功耗与发热；
* 下限 10 %：仍然能看清，但很暗。**要彻底关闭屏幕请看“背光超时”或把“启用显示屏”设为“否”**，
  用亮度 0 % 关屏会让人以为设备坏了。

调节方法：在 ETS 里改这个参数，或者用控制台 `disp bright <1..100>` 现场试出合适的值，
再把 ETS 参数按同样的数值固化下来。控制台改的值在重启后会被 ETS 参数覆盖。
